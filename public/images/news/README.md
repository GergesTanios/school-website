# News photos

Add the supplied article photographs here:

- news-1.jpg
- news-2.jpg
- news-3.jpg

No stock photos are substituted. Until a file exists, the UI shows a branded
image-pending surface. Add more files to an article's `images` array in
`src/data/news.ts` to create its gallery. The first image is the shared cover.
